# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'windows-latest'

steps:
- script: powershell -ExecutionPolicy ByPass -File .\build.ps1 -script .\build.cake -target "Build" -verbosity normal
  displayName: 'Build'
